package stepDefinitions;

import java.io.IOException;

import io.cucumber.java.Before;

public class Hooks {
	/*@Before("@DeletePlace")

	public void beforeScenario() throws IOException
	//execute this code only when place id is null
	//write a code that will give place id
	{
		System.out.println("Hooks started");
		StepDefinition sd = new StepDefinition();
       System.out.println(sd.placeid);
		
	if (sd.placeid =="null")
	{
			sd.add_place_payload_with("treewer", "treewerhouse", "treewerlang");
			sd.user_calls_add_place_api_with_http_request("AddPlaceAPI", "POST");
			sd.extract_placeid_created_using_add_place_api();
		}

	}*/

}
